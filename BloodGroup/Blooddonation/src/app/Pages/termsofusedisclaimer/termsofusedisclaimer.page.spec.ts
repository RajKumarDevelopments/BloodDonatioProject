import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TermsofusedisclaimerPage } from './termsofusedisclaimer.page';

describe('TermsofusedisclaimerPage', () => {
  let component: TermsofusedisclaimerPage;
  let fixture: ComponentFixture<TermsofusedisclaimerPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TermsofusedisclaimerPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
