import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PostabloodrequestPage } from './postabloodrequest.page';

describe('PostabloodrequestPage', () => {
  let component: PostabloodrequestPage;
  let fixture: ComponentFixture<PostabloodrequestPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PostabloodrequestPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
