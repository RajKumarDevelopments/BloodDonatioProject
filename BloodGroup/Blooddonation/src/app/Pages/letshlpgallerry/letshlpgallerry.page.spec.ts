import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LetshlpgallerryPage } from './letshlpgallerry.page';

describe('LetshlpgallerryPage', () => {
  let component: LetshlpgallerryPage;
  let fixture: ComponentFixture<LetshlpgallerryPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LetshlpgallerryPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
