import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SupportletshelpPage } from './supportletshelp.page';

describe('SupportletshelpPage', () => {
  let component: SupportletshelpPage;
  let fixture: ComponentFixture<SupportletshelpPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SupportletshelpPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
