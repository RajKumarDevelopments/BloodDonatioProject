import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PrivacypolicysidePage } from './privacypolicyside.page';

describe('PrivacypolicysidePage', () => {
  let component: PrivacypolicysidePage;
  let fixture: ComponentFixture<PrivacypolicysidePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PrivacypolicysidePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
