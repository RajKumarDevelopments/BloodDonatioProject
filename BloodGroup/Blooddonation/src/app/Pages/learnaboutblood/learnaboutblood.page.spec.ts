import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LearnaboutbloodPage } from './learnaboutblood.page';

describe('LearnaboutbloodPage', () => {
  let component: LearnaboutbloodPage;
  let fixture: ComponentFixture<LearnaboutbloodPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LearnaboutbloodPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
